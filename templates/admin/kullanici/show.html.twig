{% extends 'adminbase.html.twig' %}

{% block title %}Üye Ekleme{% endblock %}

{% block content %}
<div class="content-body">

    <div class="row page-titles mx-0">
        <div class="col p-md-0">
            <ol class="breadcrumb">
                <li class="breadcrumb-item"><a href="javascript:void(0)">Üyeler</a></li>
                <li class="breadcrumb-item active"><a href="javascript:void(0)">Üye Detayı</a></li>
            </ol>
        </div>
    </div>
    <!-- row -->

    <div class="container-fluid">
        <div class="row">

            <div class="col">
                <div class="card">
                    <div class="card-title">
                        <h3>Üye Detayı </h3>
                    </div>



    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ kullanici.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ kullanici.name }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ kullanici.email }}</td>
            </tr>
            <tr>
                <th>Password</th>
                <td>{{ kullanici.password }}</td>
            </tr>
            <tr>
                <th>Type</th>
                <td>{{ kullanici.roles[0] }}</td>
            </tr>
            <tr>
                <th>Status</th>
                <td>{{ kullanici.status }}</td>
            </tr>

        </tbody>
    </table>



                    <div class="form-group">

    <a class="btn mb-1 btn-info" href="{{ path('kullanici_index') }}">Üye listesine Dön</a>

    <a class="btn mb-1 btn-primary"href="{{ path('kullanici_edit', {'id': kullanici.id}) }}">Düzelt</a>

    {{ include('admin/kullanici/_delete_form.html.twig') }}

                    </div>
                </div>
            </div>
        </div>
    </div>
</div>


    <!-- #/ onclick="return confirm ('Silinecek emin  misin') "  container -->
{% endblock %}
