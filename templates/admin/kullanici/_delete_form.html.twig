<form method="post" action="{{ path('kullanici_delete', {'id': kullanici.id}) }}" onsubmit="return confirm('Silinecek Emin  misiniz?');">
    <input type="hidden" name="_method" value="DELETE">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kullanici.id) }}">
    <button class="btn mb-1 btn-danger" >Delete</button>
</form>
